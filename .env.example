# Local dev (SQLite). For Postgres, set the connection string here and change
# `provider` in prisma/schema.prisma to "postgresql".
DATABASE_URL="file:./dev.db"

# Password your team types on the agency login screen.
APP_SECRET="change-me"
