{
  "name": "lukreative-tracker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p 3200",
    "build": "prisma generate && next build",
    "start": "next start -p 3200",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "import": "node scripts/import-xlsx.mjs",
    "check:db": "node scripts/check-db.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "next": "^15.1.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  }
}
